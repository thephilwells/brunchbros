# Architecture — Brunch Bros

Status: **authoritative**, but currently near-empty. This describes the
architecture of the code that actually exists — never speculative or
planned architecture. Update it as real structure is built, not before.

## Current state

No source code exists yet. There is nothing to describe.

## When this gets filled in

As soon as the first ROM skeleton exists (see `docs/ROADMAP.md`), this file
should start tracking things like:
- Source file layout and what each file is responsible for
- Memory map usage (which WRAM/HRAM regions are used for what)
- ROM bank layout, once banking is in play
- Major subsystems and how they interact (once there is more than one)

Until then, keep this file short and honest about the fact that there's
nothing here yet.
